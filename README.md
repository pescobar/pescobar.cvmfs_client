pescobar.cvmfs_client
=========

Install and configure the cvmfs client on ubuntu systems

Role variables
--------------

- `cvmfs_client_eessi_version` (default `2025.06`): the EESSI version
  `enable-software-stack-eessi` sources from
  `/cvmfs/software.eessi.io/versions/`.
